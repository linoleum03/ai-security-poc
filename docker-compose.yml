services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-security-poc
    ports:
      - "8000:80"
    volumes:
      - ./database:/var/www/html/database
      - ./storage:/var/www/html/storage
    restart: unless-stopped